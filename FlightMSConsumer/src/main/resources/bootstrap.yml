spring:
  cloud:
    config:
      uri: http://localhost:8080

---
spring:
  cloud:
    consul:
      host: 127.0.0.1
      port: 5000
server:
  port: 8070
  
---
spring:
  application:
    name: flight-microservice-consumer